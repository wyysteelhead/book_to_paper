# Document Lab v0.1.2

Document Lab 是一个本地优先的桌面阅读器，可以把 EPUB 和 TXT 书籍转换成论文风格的阅读界面，适合在工作屏幕上更安静地阅读。

## 本次更新

- 修复 EPUB 原书目录识别：目录锚点缺失时不再错误回退到假章节顺序。
- 拆分“论文小节”和“原书章节”：论文继续按假章节分页和展示，原书章节只作为正文中的来源章节标题，并用于原书章节抽屉导航。
- 优化阅读器进入文章、滚动、顶部栏显隐和按钮点击时的响应速度。
- 新增文章打开提示和页面局部加载提示，避免长文加载时看起来像卡死。
- 优化局部页面渲染窗口，阅读时会按当前位置预加载附近页面。
- 补充屏蔽词说明：屏蔽词只影响统计和图表，不会修改正文内容。
- 移除不再使用的自定义图表模板预览项。

## 安装包

通过 GitHub Actions 构建时，本版本会生成：

- macOS：`.dmg`
- Windows：`.exe` NSIS 安装包

## 说明

- 书籍解析和渲染都在本地完成。
- Windows 安装包由 Windows GitHub Actions runner 生成。
- macOS 构建默认未签名，除非仓库中配置了 Apple 签名密钥。
